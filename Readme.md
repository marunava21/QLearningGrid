Instructions:

```shell
- Go to the m files folder
- run the task_main.m
- it will ask for user defined input
Depending on the user input it will run the task1 or task2 with eithe the given reward matrix or the new matrix.
Also the gamma value can be provided by user input.
```

